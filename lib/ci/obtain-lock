#!/bin/bash
set -euo pipefail

HERE="$(cd "$(dirname "$0")"; pwd)"

"$HERE/"../lib/tf-lock/tf-lock "$@"
status=$?

set -x
if [ $status -eq 0 ]; then
  echo "lock_obtained=true"
else
  echo "lock_obtained=false"
  exit 1
fi
