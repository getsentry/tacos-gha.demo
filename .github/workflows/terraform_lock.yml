name: Terraform Lock

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  terraform_lock:
    uses: getsentry/tacos-gha/.github/workflows/tacos_lock.yml@bukzor/import-workflows
    with:
      #tacos_gha_ref: refs/heads/stable
      tacos_gha_ref: refs/heads/bukzor/import-workflows
