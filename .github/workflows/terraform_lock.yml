name: Terraform Lock

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_lock.yml@tacos-apply
    with:
      tacos_gha_ref: refs/heads/tacos-apply
