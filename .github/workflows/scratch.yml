name: Scratch

on:
  workflow_dispatch: null
  pull_request:
    types: [synchronize]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/scratch.yml@emarks/avoid-lock-on-draft
    with:
      tacos_gha_ref: refs/heads/emarks/avoid-lock-on-draft
