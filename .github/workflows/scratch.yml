name: Scratch

on:
  workflow_dispatch: null
  pull_request:
    types: [synchronize]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/scratch.yml@bukzor/lock-on-pr
    with:
      tacos_gha_ref: refs/heads/bukzor/lock-on-pr
