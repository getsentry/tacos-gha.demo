name: Scratch

on:
  workflow_dispatch: null
  pull_request:
    types:
      - labeled

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/scratch.yml@bukzor/our-own-pr-tags
    with:
      tacos_gha_ref: refs/heads/bukzor/our-own-pr-tags
