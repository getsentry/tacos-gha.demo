name: Terraform Apply

on:
  workflow_dispatch: null
  pull_request:
    types:
      - labeled

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_apply.yml@kneeyo1/fix-tf-lock-acquire
    with:
      tacos_gha_ref: refs/heads/kneeyo1/fix-tf-lock-acquire
