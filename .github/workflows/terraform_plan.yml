name: Terraform Plan

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  terraform_plan:
    uses: getsentry/tacos-gha/.github/workflows/tacos_plan.yml@bukzor/import-workflows
    with:
      #tacos_gha_ref: refs/heads/stable
      tacos_gha_ref: refs/heads/bukzor/import-workflows
