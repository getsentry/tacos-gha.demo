name: Terraform Plan

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_plan.yml@bukzor/fix/unlock_again
    with:
      tacos_gha_ref: refs/heads/bukzor/fix/unlock_again
