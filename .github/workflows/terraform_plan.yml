name: Terraform Plan

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_plan.yml@emarks/update-plan-message
    with:
      tacos_gha_ref: refs/heads/emarks/update-plan-message
