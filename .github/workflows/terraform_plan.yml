name: Terraform Plan

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_plan.yml@emarks/lock-and-plan-on-ready
    with:
      tacos_gha_ref: refs/heads/emarks/lock-and-plan-on-ready
