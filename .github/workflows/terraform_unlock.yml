name: Terraform Unlock

on:
  workflow_dispatch: null
  pull_request:
    types:
      - closed
      - converted_to_draft
      - labeled

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_unlock.yml@kneeyo1/fix-tf-lock-acquire
    with:
      tacos_gha_ref: refs/heads/kneeyo1/fix-tf-lock-acquire
