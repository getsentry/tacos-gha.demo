name: Terraform Unlock

on:
  workflow_dispatch: null
  pull_request:
    types: [labeled]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_unlock.yml@bukzor/fix/unlock_again
    with:
      tacos_gha_ref: refs/heads/bukzor/fix/unlock_again
