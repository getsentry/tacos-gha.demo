name: Terraform Unlock

on:
  workflow_dispatch: null
  pull_request_target:
    types:
      - closed
      - converted_to_draft
      - labeled

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_unlock.yml@main
    with:
      tacos_gha_ref: refs/heads/main
