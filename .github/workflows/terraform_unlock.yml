name: Terraform Unlock

on:
  workflow_dispatch: null
  pull_request:
    types: [labeled]

jobs:
  terraform_unlock:
    uses: getsentry/tacos-gha/.github/workflows/tacos_unlock.yml@bukzor/import-workflows
    with:
      #tacos_gha_ref: refs/heads/stable
      tacos_gha_ref: refs/heads/bukzor/import-workflows
