name: Terraform Unlock

on:
  workflow_dispatch: null
  pull_request:
    types: [labeled]

jobs:
  tacos-gha:
    uses: getsentry/tacos-gha/.github/workflows/tacos_unlock.yml@emarks/update-plan-message
    with:
      tacos_gha_ref: refs/heads/emarks/update-plan-message
